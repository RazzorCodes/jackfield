[build-system]
requires = ["maturin>=1.4,<2"]
build-backend = "maturin"

[project]
name = "jackfield"
requires-python = ">=3.8"
dynamic = ["version"]

[tool.maturin]
features = ["python"]
bindings = "pyo3"
